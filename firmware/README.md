# Custom T-TWR firmware (not yet written)

Planned: PlatformIO project, `board = LilyGo-T-TWR-Plus`, reusing
`LilyGo_TWR_Library` from Xinyuan-LilyGO/T-TWR. See `docs/architecture.md`
for the serial protocol. Stage 1 build compiles with `-DTWR_TX_DISABLED`.

`factory/` holds the as-shipped firmware images.
